#!/bin/sh

# Source configuration and variables
. ../../lib/bsfl
. ../../lib/global-functions
. ../../lib/global-variables


if [ -f "/etc/hosts" ]
then
	$CAT /etc/hosts 
else
	$ECHO "Could not find /etc/hosts"
fi
